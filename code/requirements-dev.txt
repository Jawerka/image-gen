# Development dependencies (install in addition to requirements.txt)
# Usage: pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
httpx>=0.27        # Required by FastAPI TestClient
ruff>=0.9           # Fast linter + formatter