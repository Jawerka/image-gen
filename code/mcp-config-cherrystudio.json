{
  "mcpServers": {
    "image-gen-pro": {
      "type": "streamableHttp",
      "url": "http://192.168.88.16:8081/mcp",
      "headers": {
        "Content-Type": "application/json"
      }
    }
  },
  "_instructions": {
    "generate_image": {
      "description": "Generates images via Stable Diffusion WebUI. For multiple images, call the tool multiple times.",
      "examples": {
        "single_image": "generate_image(prompt='a beautiful landscape')",
        "with_negative": "generate_image(prompt='...', negative_prompt='blurry, low quality')"
      },
      "note": "Each call generates one image. Use seed=-1 for random or specify seed for reproducibility."
    }
  }
}
